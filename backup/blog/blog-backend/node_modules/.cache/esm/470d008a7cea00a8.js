let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_7ce‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_7ce‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_7ce‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_7ce‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_7ce‍.w("./api",[["default",["api"],function(v){api=v}]]);_7ce‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
.connect(MONGO_URI, {})
 .then(()=> {
    console.log('Connected to MongoDB');
 })
 .catch((e) => {
    _7ce‍.g.console.error(e);
 });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api라우트 적용

// 라우더 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되어 있지 않다면 400을 사용
const port = PORT || 4000;
app.listen(port, () => {
    _7ce‍.g.console.log('Listening to port %d', port);
});